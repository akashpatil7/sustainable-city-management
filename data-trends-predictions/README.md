## Python Microservice for Trends and Predictions

Initial Setup:

`pip install -r requirements.txt`


Configure the flask server:

`export FLASK_APP=./src/app.py`


Run the flask server:

`flask run`